### OpenAI-compatible LLM config (local only)
### Copy to `.env` and fill in your real key.
### Never commit `.env` (it's ignored via `.gitignore`).

# OPENAI_BASE_URL=https://openai.rc.asu.edu/v1
# OPENAI_API_KEY=your_api_key_here

